---
description: Disable a memory entry so it's no longer auto-injected.
argument-hint: <id>
---

Disable memory entry: $ARGUMENTS

```bash
node "${CLAUDE_PROJECT_DIR}/.claude/hooks/memory-cli.cjs" disable "$ARGUMENTS"
```
