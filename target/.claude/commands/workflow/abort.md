---
description: Abort the active workflow after confirming with the user.
---

Confirm with the user, then abort the active harness workflow.

```bash
node "${CLAUDE_PROJECT_DIR}/.claude/hooks/workflow-cli.cjs" abort
```
